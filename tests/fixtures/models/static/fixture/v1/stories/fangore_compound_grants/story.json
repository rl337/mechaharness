{
  "id": "fangore_compound_grants",
  "persona": "fangore",
  "title": "Compose read-only and write permission packs",
  "kind": "grant_gate_write",
  "narrative": "Fangore\u2019s host app must not let agents write the filesystem by default. A read-only pack refuses the write tool; composing it with a write pack allows the same tool. He needs reusable permission packs instead of copy-pasted lists.",
  "implementation": "`AccessPolicy` deny-by-default grants; `CompoundPolicy` unions packs; `AccessControl` enforces tool grants and emits access-check events. This is the permission half of tool gating \u2014 every tool call must pass access before side effects.\n\nCovers: host compound grants.",
  "validation": "Kind `grant_gate_write` \u2192 `_run_grant_gate_write`. Unit: `tests/test_access.py`.",
  "footnotes": []
}
